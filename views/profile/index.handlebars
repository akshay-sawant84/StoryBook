{{!-- <div class="py-5">
  <div class="row">
    <div class="col-md-6">
      <div class="d-flex flex-column justify-content-center align-items-center">
        <img src="{{data.image}}" alt="user image" class="user-image">
        <h4 class="my-3">{{data.name}}</h4>
        <div>
          <p class="lead px-2"><i class="fas fa-envelope"></i> {{data.email}}</p>
        </div>
      </div>
    </div>
    <div class="col-md-6">
      <h3>User Stories</h3>
      <ul class="list-group">
        {{#each content}}
        <li class="list-group-item">
          <h6 class="m-0"><a href="/story/{{_id}}">{{title}}</a></h>
        </li>
        {{else}}

        {{/each}}
      </ul>
    </div>
  </div>
</div> --}}

<div>
  <h2 class="my-3">Welcome {{user.firstName}}</h2>
  <h6 class="my-3">Here are your stories</h6>
</div>
{{#if stories}}
<table class="table table-striped">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Title</th>
      <th scope="col">Date</th>
      <th scope="col">Status</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    {{#each stories}}
    <tr>
      <th scope="row"><a href="/story/{{_id}}">{{title}}</a></th>
      <td>{{formatDate createdAt 'MMMM Do YYYY, h:mm:ss a'}}</td>
      <td>{{status}}</td>
      <td>
        <div>
          <a class="mr-2 btn btn-warning" href="/edit/{{_id}}" role="button"><i class="fas fa-edit"></i></a>
          <form class="d-inline" action="/story/{{_id}}?_method=DELETE" method="POST">
            <input type="hidden" name="_method" value="DELETE">
            <button class="btn btn-danger" role="button"><i class="fas fa-trash"></i></button>
          </form>
        </div>
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>

{{else}}
<p>You have not created any stories</p>
{{/if}}